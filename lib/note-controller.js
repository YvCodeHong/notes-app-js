// window.onload = function() {
//   // var id = document.getElementById('app');
//   //
//   // id.innerHTML = "howdy";
// }

// Take note list model as a parameter
// Adds a note that says anything
// Creates a note list view, passingin the note list model
// Note controller has method that gets HTML from note list view and inserts it into app element

(function(exports) {
  function NoteController(notelist) {

  };

  exports.NoteController = NoteController;
})(this);
